# Stride28 Requirements
# Python >= 3.10

# ============================================
# LangChain 核心（简历关键词 ✔️）
# ============================================
langchain==0.3.17
langchain-core==0.3.31
langchain-community==0.3.16
langchain-text-splitters==0.3.5
langchain-chroma==0.2.2

# ============================================
# LangGraph（Checkpoint 2 后必做）
# ============================================
langgraph==0.2.68

# ============================================
# LangSmith 可观测性（必做）
# ============================================
langsmith==0.2.11

# ============================================
# 向量存储
# ============================================
chromadb==0.6.3

# ============================================
# Tongyi/DashScope（主要 Provider）
# ============================================
dashscope==1.22.1

# ============================================
# Web 框架
# ============================================
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9
aiofiles>=24.1.0

# ============================================
# Markdown 渲染
# ============================================
markdown>=3.5.1

# ============================================
# 浏览器自动化（搜索模块）
# ============================================
playwright>=1.49.0

# ============================================
# PDF 解析
# ============================================
pdfplumber>=0.11.0
PyMuPDF>=1.24.0

# ============================================
# 文档解析（Word .docx）
# ============================================
python-docx==1.1.2

# ============================================
# 测试
# ============================================
hypothesis>=6.0.0

# ============================================
# 工具库
# ============================================
python-dotenv==1.0.1
pydantic==2.10.6
rich==13.9.4
httpx==0.28.1
openai>=1.50.0

# ============================================
# 本地 Embedding（已移除，改用 DashScope API）
# ============================================
# sentence-transformers>=2.2.0  # 依赖 torch ~1.5GB，暂不需要
PyExecJS>=1.5.1

# ============================================
# Reranker（可选：不安装时 reranker 自动禁用，降级为 embedding-only）
# ============================================
# FlagEmbedding>=1.2.0  # pip install FlagEmbedding（首次调用自动下载模型 ~1.1GB）